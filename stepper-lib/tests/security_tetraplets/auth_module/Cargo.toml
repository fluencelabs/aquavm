[package]
name = "auth_module"
version = "0.1.0"
authors = ["Fluence Labs"]
edition = "2018"

[[bin]]
name = "auth_module"
path = "src/main.rs"

[dependencies]
stepper-lib = { path = "../../../../stepper-lib" }
fluence = { version = "=0.2.12", features = ["logger"] }

[workspace]
