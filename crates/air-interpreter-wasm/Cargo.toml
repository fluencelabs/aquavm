[package]
name = "air-interpreter-wasm"
version = "0.0.6"
authors = ["Fluence Labs"]
description = "Distribution of AIR interpreter as .wasm"
license = "Apache-2.0"
edition = "2018"
include = [
    "**/*.rs",
    "Cargo.toml",
    "aquamarine.wasm"
]

[build-dependencies]
tinyjson = "2.2.0"
